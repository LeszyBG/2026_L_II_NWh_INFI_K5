version: 2.1

jobs:
  build-and-test:
    machine:
      image: ubuntu-2004:current

    steps:
      - checkout
      - run:
          command: make deps
      - run:
          command: make lint
      - run:
          command: make test
      - run:
          command: make docker_build

workflows:
  basic:
    jobs:
      - build-and-test